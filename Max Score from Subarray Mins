class Solution {
    public int maxSum(int arr[]) {
        // code here

        
        int cnt=0;
        
    for(int i=1 ; i<arr.length ; i++){
        cnt=Math.max(cnt, arr[i-1]+arr[i]);
    }
       return cnt;
      
    }
}
